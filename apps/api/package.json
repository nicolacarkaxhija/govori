{
  "name": "@govori/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "description": "Govori API server",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run --coverage",
    "dev": "node --watch --experimental-strip-types src/main.ts"
  },
  "dependencies": {
    "@fastify/swagger": "^9.8.1",
    "@govori/config": "workspace:*",
    "@govori/content": "workspace:*",
    "@govori/srs": "workspace:*",
    "@govori/transliteration": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.7.1",
    "fastify-type-provider-zod": "^7.0.0",
    "postgres": "^3.4.9",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^12.0.4",
    "drizzle-kit": "^0.31.10"
  }
}
